PORT=4000
MONGODB_URI=mongodb://localhost:27017/paper-paints

# JWT/session signing (or falls back to ADMIN_PASSWORD)
SESSION_SECRET=your-session-secret

# First-time admin: only used when no admins exist in DB (bootstrap)
ADMIN_EMAIL=admin@example.com
ADMIN_PASSWORD=12345678
