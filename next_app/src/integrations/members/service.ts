import type { Member } from "./types";

/**
 * Placeholder for member auth. Wix members removed.
 * Admin auth will be added in Phase 2/3 via /admin.
 */
export const getCurrentMember = async (): Promise<Member | null> => {
  return null;
};
